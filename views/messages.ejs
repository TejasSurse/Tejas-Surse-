<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Contact Messages - Tejas Surse</title>
    <script src="https://cdn.tailwindcss.com/3.4.16"></script>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600&family=Poppins:wght@400;500;600&display=swap" rel="stylesheet">
    <style>
        body { font-family: 'Inter', sans-serif; }
        .message-card {
            backdrop-filter: blur(12px);
            background: rgba(255, 255, 255, 0.8);
            box-shadow: 0 8px 32px rgba(0, 0, 0, 0.1);
        }
    </style>
    <script>tailwind.config = { theme: { extend: { colors: { primary: '#1B1B3A', secondary: '#FFD700' } } } }</script>
</head>
<body class="bg-[#F8F8F8] min-h-screen p-8">

    <main class="min-h-screen flex items-center justify-center">
        <div class="w-full max-w-4xl mx-auto">
            <h1 class="font-['Poppins'] text-4xl font-semibold text-primary mb-6 text-center">Contact Messages</h1>
            <% if (messages.length > 0) { %>
                <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
                    <% messages.forEach(function(message) { %>
                        <div class="message-card p-6 rounded-2xl">
                            <h3 class="font-['Poppins'] text-xl font-medium text-primary mb-2"><%= message.name %></h3>
                            <p class="text-gray-600 mb-1"><strong>Email:</strong> <%= message.email %></p>
                            <p class="text-gray-800 mb-4"><%= message.message %></p>
                            <p class="text-gray-500 text-sm">Received at: <%= new Date(message.receivedAt).toLocaleString() %></p>
                        </div>
                    <% }); %>
                </div>
            <% } else { %>
                <div class="text-center text-gray-500 mt-12">
                    <p class="text-lg">No messages received yet.</p>
                </div>
            <% } %>
        </div>
    </main>
</body>
</html>
